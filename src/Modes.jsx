const modes = [
 ['① → ②','Sequential wheels','Pick a group, then an option from only that group.'],['◎ ◎','Linked wheels','Spin group and option wheels at the same time.'],['◉','Nested wheels','Inner and outer rings reveal a paired choice.'],['✹','Giant wheel','Every group-and-option pairing on one wheel.'],['🏆','Tournament','Choices compete through an elimination bracket.'],['▥','Slot reels','Group and option reels stop in sequence.'],['⌄⌄⌄','Cascading','Make a choice at each level of a decision tree.'],['◌ ◌ ◉','Group playoffs','Spin each group’s option, then choose a group.'],['✕ ◯','Knockout spins','Remove each selection until one remains.'],['Ⅰ Ⅱ Ⅲ','Ranked pointers','Multiple pointers produce a ranked shortlist.']
];
export function Modes(){return <section className="modes" aria-labelledby="modes-title"><span className="eyebrow">TEN WAYS TO DECIDE</span><h2 id="modes-title">Spin modes</h2><p className="muted">The same configuration can power every decision style.</p><div className="mode-grid">{modes.map((m,i)=><article className={`mode-card ${i===0?'active':''}`} key={m[1]}><div className={`mode-demo demo-${i}`} aria-hidden="true"><span>{m[0]}</span></div><div><h3>{m[1]} {i===0&&<em>DEFAULT</em>}</h3><p>{m[2]}</p></div></article>)}</div></section>}
